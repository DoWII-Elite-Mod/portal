---
import HeroCard from './HeroCard.astro';

const cards = [
  {
    header: 'current elitemod patch',
    title: 'v2.9.8.4 Released',
    dateTime: 'January 3rd, 2023'
  },
  {
    header: '$1000 1v1 Tournament',
    title: 'Faction Waaagh!',
    dateTime: 'January 20th - 22nd'
  }
];
---

<section class="mb-4 md:mb-8">
  <div class="mb-0 flex flex-col gap-3 sm:mb-0 sm:gap-8 lg:flex-row">
    <div
      class="relative flex h-auto w-full basis-1/3 overflow-hidden rounded-2xl border-2 md:basis-1/2 lg:basis-3/5 xl:basis-3/5"
    >
      <div
        class="absolute inset-0 bg-promo-one bg-cover bg-no-repeat opacity-30"
      >
      </div>
      <div class="relative flex flex-col justify-evenly gap-6 p-8">
        <h1 class="text-4xl font-bold tracking-tight text-zinc-100 sm:text-5xl">
          Elitemod Official
        </h1>
        <p class="mt-6 text-base">
          Elite is a multiplayer modification (mod) for Warhammer 40,000 Dawn of
          War II: Retribution. “Mod” means that we players have changed, added
          and removed aspects of the retail game to make its more interesting.
        </p>
      </div>
    </div>
    <div
      class="flex w-full grow-0 basis-2/3 flex-col gap-3 sm:basis-1/2 sm:flex-row sm:gap-8 lg:basis-2/5 lg:flex-col"
    >
      {
        cards.map(({ header, title, dateTime }) => (
          <HeroCard header={header} title={title} dateTime={dateTime} />
        ))
      }
    </div>
  </div>
</section>
